# 2026-04-26 工作日志

## LabVIEW CLF 配置指南
- 创建了 `LabVIEW_CLF_配置指南.md`，详细说明三个 DLL 函数的 CLF 节点配置
- 确认 DLL 导出函数名无装饰（InitDetector / ProcessImage / ReleaseDetector），使用 cdecl 调用约定
- 关键注意：qrTexts 预分配 20480 字节 U8 数组，坐标数组预分配 10 个 I32
- 单值输出(outWidth/qrCount等)用 Numeric→Pointer，数组输出用 Array→Data Pointer

## API v2 重构（简化CLF配置）
- 从 3 函数 16 参数 → 7 函数 每个最多 5 参数
- 新函数列表：InitDetector / ProcessImageFile / ProcessImageData / GetQRText / GetQRRect / GetAnnotatedImagePath / ReleaseDetector
- 标注图自动保存到 %TEMP%\qr_annotated.png，不再需要 outputPath 参数
- GetAnnotatedImagePath 返回固定路径，LabVIEW 用 IMAQ Read File 读取
- 识别逻辑完全不变，批量测试 93.6% 识别率一致
- LabVIEW 2025 64-bit + DLL 64-bit 位数匹配确认
