# 2026-05-04 工作记录

## 文档全面更新

用户要求检查 DESIGN.md 和 PROJECT_DETAIL.md 与代码的差异，发现严重过时。

### DESIGN.md 问题
- 接口写旧版13函数，当前是7函数
- QRDetectorState 有 VideoCapture/Statistics，当前没有
- 返回码不同
- 没有 processFrameFast / colorOrder=2 / 标注图保存到文件的设计
- 全文重写为 v2.0

### PROJECT_DETAIL.md 问题
- 接口写旧版13函数(LoadVideo/GetVideoInfo/SeekFrame/GetStatistics等已删除)
- 缺少当前函数(GetQRText/GetQRRect/GetAnnotatedImagePath)
- 编译命令过时(cmd /c build.bat → bash 直接 g++)
- 调用流程过时
- 全文重写为 v3.0

### QRDecoder_内部逻辑说明.md
- 补充 isDuplicateQR 去重函数、MAX_QR_COUNT 限制
- 补充 ZBar 完整配置详情
- 修正 GetAnnotatedImagePath 不加锁的描述
- 补充 5 种策略配置参数表
- 新增返回值定义表

### QRDecoder.h 修正
- "6 个函数" → "7 个函数"
- %TEMP%\qr_annotated.png → 完整 BMP 路径（2处）
- "预处理(CLAHE+锐化)" → "多策略预处理"
